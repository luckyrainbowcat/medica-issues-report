# MongoDB (ไม่ใช้แล้ว - ใช้ Firebase แทน)
# MONGODB_URI=mongodb://localhost:27017/issue-tracker

# Prisma Database URL (ไม่ใช้แล้ว - ใช้ Firebase Firestore แทน)
# DATABASE_URL="file:./dev.db"

# Firebase Configuration (ตั้งค่าใน src/lib/firebase.ts)
# Firebase config ถูกตั้งค่าในไฟล์ firebase.ts แล้ว

# ImgBB API (สำหรับอัปโหลดรูปภาพ - ฟรี 100%)
# ไปที่ https://imgbb.com → สมัครสมาชิก (ฟรี) → Settings → API Key
# Copy API Key มาใส่ด้านล่าง
# IMGBB_API_KEY=your_imgbb_api_key_here

# Firebase Admin SDK (ไม่ใช้แล้ว - ใช้ ImgBB API แทน)
# สำหรับ Production: สร้าง Service Account Key จาก Firebase Console
# ไปที่ Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# Copy JSON content ทั้งหมดมาใส่ใน FIREBASE_SERVICE_ACCOUNT_KEY (เป็น JSON string)
# หรือเก็บเป็นไฟล์และตั้งค่า GOOGLE_APPLICATION_CREDENTIALS
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"medica-issuev2",...}

# MinIO Configuration (ไม่ใช้แล้ว - ใช้ ImgBB API แทน)
# MINIO_ENDPOINT=localhost
# MINIO_PORT=9000
# MINIO_USE_SSL=false
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=issue-uploads
# MINIO_PUBLIC_URL=http://localhost:9000

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Service Management Project Port
NEXT_PUBLIC_SERVICE_MANAGEMENT_PORT=3001

